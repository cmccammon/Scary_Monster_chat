
<h5>Leave a message in our chatroom</h5>
<div class="container" style="border:1px solid black">
  <table>
    <thead>
      <tr>
        <th>Messages</th>
        <th>User</th>
        <th>Time posted</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @chats.each do |chat| %>
        <tr>
          <td><%= chat.messages %></td>
          <td><%= chat.user.username %></td>
          <td><%= chat.created_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<br>
<div class="container">
  <!-- <%# react_component ("NewChatMessage") %> -->

  <%= render 'form' %>
</div>
