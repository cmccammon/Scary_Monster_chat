<%= form_for(@chat) do |f| %>
  <% if @chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chat.errors.count, "error") %> prohibited this chat from being saved:</h2>

      <ul>
      <% @chat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Type your chat message here; field cannot be empty" %><br>
    <%= f.text_field :messages %>
  </div>
  <div class="actions">
    <button class="btn waves-effect waves-light" id='newMessage'>Submit</button>
  </div>
<% end %>
